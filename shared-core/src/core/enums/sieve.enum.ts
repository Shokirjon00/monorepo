export enum SieveOperators {
  equals = '==*',
  equalsOnly = '==',
  notEquals = '!=*',
  greaterThan = '>*',
  lessThan = '<*',
  greaterThanOrEqual = '>=',
  lessThanOrEqual = '<=*',
  contains = '@=*',
  startsWith = '_=*',
  notContains = '!@=*',
  notStartsWith = '!_=*'
}
