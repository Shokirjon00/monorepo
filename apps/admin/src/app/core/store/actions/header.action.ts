/*
import {createAction, props} from '@ngrx/store';

export const setCompanyId = createAction(
  '[HEADER] Set Company ID', props<{ id?: string }>()
);

export const cleanCompanyId = createAction(
  '[HEADER] Clean Company ID'
);

export const setMerchantId = createAction(
  '[HEADER] Set Merchant ID', props<{ id?: string }>()
);

export const cleanMerchantId = createAction(
  '[HEADER] Clean Merchant ID'
);

export const setPosId = createAction(
  '[HEADER] Set Pos ID', props<{ id?: string }>()
);

export const cleanPosId = createAction(
  '[HEADER] Clean Pos ID'
);

export const HeaderAction = {
  setCompanyId,
  cleanCompanyId,
  setMerchantId,
  cleanMerchantId,
  setPosId,
  cleanPosId
}
*/
