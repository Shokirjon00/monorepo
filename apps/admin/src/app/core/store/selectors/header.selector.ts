/*
import {createFeatureSelector, createSelector} from '@ngrx/store';
import {IHeaderModel} from '@core/store/models/header.model.interface';

export const headerFeatureSelector = createFeatureSelector<IHeaderModel>('header');

export const getCompanyId = createSelector(
  headerFeatureSelector,
  (header) => header.companyId);

export const getMerchantId = createSelector(
  headerFeatureSelector,
  (header) => header.merchantId);

export const getPosId = createSelector(
  headerFeatureSelector,
  (header) => header.posId);
*/
