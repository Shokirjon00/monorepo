import { Routes } from "@angular/router";
import { PosTerminalSettingComponent } from "@modules/client/pos-terminal-setting/pos-terminal-setting.component";

export const POS_TERMINAL_SETTING_ROUTES: Routes = [
  {
    path: '',
    component: PosTerminalSettingComponent,
  },
]
